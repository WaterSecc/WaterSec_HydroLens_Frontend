import { motion } from 'framer-motion'
import { Activity, Boxes, CheckCircle2, Gauge, Send } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'
import { KpiCard } from '@/components/data-display/kpi-card'
import { StatusChip } from '@/components/data-display/status-chip'
import { EmptyState } from '@/components/feedback/empty-state'
import { FormField } from '@/components/forms/form-field'
import { Container } from '@/components/layout/container'
import { PageHeader } from '@/components/layout/page-header'

export function FoundationPage() { return <Container className="py-12 sm:py-16"><motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .4 }} className="space-y-(--section-gap)"><PageHeader eyebrow="Frontend foundation" title="Built for intelligent water operations." description="A reusable, accessible design system ready for every HydroLens workflow—without coupling presentation to business features." actions={<><Badge variant="success">Foundation ready</Badge><Button size="sm"><Send />Primary action</Button></>} /><section aria-labelledby="metrics-title"><h2 id="metrics-title" className="mb-4 text-sm font-semibold">Reusable KPI cards</h2><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><KpiCard title="System readiness" value="100" unit="%" change={4.2} icon={Gauge} description="foundation checks" /><KpiCard title="UI primitives" value="12" icon={Boxes} description="ready to compose" /><KpiCard title="API status" value="Connected" icon={Activity} description="typed Axios client" /></div></section><section className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Form foundation</CardTitle><CardDescription>Accessible controls ready for React Hook Form and Zod schemas.</CardDescription></CardHeader><CardContent className="grid gap-5"><FormField id="workspace-name" label="Workspace name" required description="Use a clear, recognizable name."><Input id="workspace-name" placeholder="WaterSec Operations" /></FormField><FormField id="notes" label="Notes"><Textarea id="notes" placeholder="Add context for your team…" /></FormField><div className="flex flex-wrap gap-2"><Button>Save changes</Button><Button variant="outline">Cancel</Button><Button variant="ghost">Learn more</Button></div></CardContent></Card><Card><CardHeader><CardTitle>States and semantics</CardTitle><CardDescription>Consistent feedback for operational interfaces.</CardDescription></CardHeader><CardContent className="space-y-6"><div className="flex flex-wrap gap-2"><StatusChip status="operational" /><StatusChip status="warning" /><StatusChip status="critical" /><StatusChip status="inactive" /><StatusChip status="info" /></div><div className="flex flex-wrap gap-2"><Badge>Default</Badge><Badge variant="secondary">Secondary</Badge><Badge variant="outline">Outline</Badge><Badge variant="warning">Review</Badge></div><EmptyState icon={CheckCircle2} title="Ready for feature modules" description="Business pages can now be composed from the shared foundation." /></CardContent></Card></section></motion.div></Container> }
