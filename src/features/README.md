# Feature modules

Add business capabilities here as isolated vertical slices. A feature may own its API functions, components, hooks, schemas, and types; shared primitives remain under `src/components`.
